
public class Sword extends Weapon
	{
	public Sword(String in, int v, int d, boolean ie, String it)
		{
		setItemName(in);
		setValue(v);
		setDamage(d);
		setIsEquipped(ie);
		setItemType(it);
		}
	}
